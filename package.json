{
  "name": "parse-this",
  "version": "0.0.3",
  "description": "Parse stringified data to corresponding types",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/m4nuC/parse-this"
  },
  "scripts": {
    "test": "NODE_ENV=test mocha specs.js --harmony --reporter spec --bail",
    "cover": "NODE_ENV=test istanbul cover -x specs.js _mocha specs.js -- --reporter spec --bail"
  },
  "keywords": [
    "parse",
    "types",
    "json"
  ],
  "author": "Emanuel Chappat <emmanuel.chappat@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "jsfp": "^0.1.0"
  },
  "devDependencies": {
    "expect": "^1.13.4",
    "istanbul": "*",
    "mocha": "*"
  }
}
